package enums;

/**
 * Esta clase se en carga de definir el enum Arquitectura
 * @author Emmanuelito18
 */

/**
 * Enum para las arquitecturas de procesador soportadas por esptool
 */
public enum Arquitectura {
    X86,X64,ARM32,ARM64,DESCONOCIDA
}
